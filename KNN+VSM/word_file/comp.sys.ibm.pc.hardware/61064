from iisakkil mika iisakkila subject re scsi v ide randy write do all scsi card for do system require a separate device driver to be load into memory for each scsi device hook up no all that i see have also an on-board bio which enable you to use up to hard drife directly under do drife be a do limitation and you have the same problem with ide and all other standard for that matter software driver often allow for better performance though you have to use them if you want to use other device beside hard disk or have more than disk will thi also be true of the o obviously these be not able to use the real mode biosis that be write for do so you need software driver that not a big deal a long a the driver be available because you wo have to fight with any low memory problem either segmented memory help structure software 