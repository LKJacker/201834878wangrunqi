from subject re no boot disk require if you do want to load in article sm stephen mark sanderson write from sm stephen mark sanderson subject no boot disk require if you do want to load date apr everybody do user in particular take note if you want to play game that their own upper memory manager do be not a problem no boot disk require a ymy system start up hit the key thi tell it to bypas config and autoexec altogether you get a plain generic session of do with nothing load there another function key that actually step thru ask if you want to execute each line but i forget it at the moment you can try find it i think it no you need not bypas the in do there be a function of multi-config have you try the multi-config be the kind that you can choose you at the startup and i find that be very good it ha no conflict to qemm i have problem when use the key you say be which be trace the step by step sorry if any error phillip phillipau 