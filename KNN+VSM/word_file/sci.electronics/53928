from uli uli allgeier subject re pad question how do i go from a schematic pcb hi in article tall cool one write after i have produce a schematic with pads-logic how do i import it into pads-pcb to create a pcb pattern the only way i get it to work be to output a futurenet netlist and then import thi into pads-pcb is there another way of do thi i do see any information in the instruction provide but i might have miss something any suggestion would be greatly appreciate thank sorry it german but i hope you understand it uebertragung der schaltplan-daten sch in die board-daten job pads-logic report net list select pads-pcb net list output file name filename angeben pads-pcb ascius in ascius input file name filename angeben error aoutput file name filename angeben nicht cr jetzt sind alle bauteile auf einem haufen und muessen mit move verteilt werden viele gruesse uli 