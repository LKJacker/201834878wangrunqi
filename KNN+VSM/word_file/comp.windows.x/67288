from rj robert joop subject re tvtwm xsetroot and sparc keyboard barr david barr write did you install the sunkbd patch it in the contrib directory on export all the key on my keyboard send event properly except the follow the end pageup pagedown on the cluster be recognize even the compose key work though i ca seem to get the compose character in an xterm to get pass anyone have a fix for the last two fix the table in or use xmodmap put stty setenv setenv lesscharset in ymy login the first prevent the strip of bite the second set the locale the third make le show the character instead of the octal representation rj robert joop rj de 