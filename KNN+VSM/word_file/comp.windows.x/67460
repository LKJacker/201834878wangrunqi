from dlemoin d lemoine subject colormap problem i be save an image on one machine and redisplaying the image on another machine both be hp model the image be create use xcreateimage and xgetimage and display with xputimage the image be redisplayed correctly except that the color be wrong because the server on the other machine be use a different colormap i try save the colormap pixel and rgb value and on the redisplay perform a table lookup against the new colormap thi do work because some rgb combo do exist in the new colormap is there a way to force the server to load color into set pixel value or be there a simpler way to solve thi problem i try use xinitcolormap but could get that to work either any help would be appreciate don lemoine naval surface warfare center dahlgren division dahlgren va dlemoin 