from lindholm george lindholm subject re xv ha escape in article lindholm george lindholm write in article bradley john bradley write no not another false alarm not a it certainly be do by next week message no thi be the real thing i repeat thi be not a drill batten down the hatch hide the woman and lock up the cow xv ha finally escape i wa clean it cage thi morning when it overpower me break down the office door and flee the lab it wa last see head in the general direction of at nearly per second if find it answer to the name of have a blast i off to the vacation capital of the waco texa and here be the first patch the follow fix some problem with the imakefile file and make it possible to compile under solari without have to use the ucb stuff oop i do get the signal process right that what i get from do the work on an ascius terminal here be the fix patch imakefile program xv bggen vdcomp xcmap if define havejpeg define havetiff ifdef havejpeg program xv bggen vdcomp xcmap all echo echo did you remember to depend first echo if define havejpeg define havetiff ifdef havejpeg subdir tiff endif define passcdebugflag makesubdir subdir dependsubdir dubdir endif all echo echo did you remember to depend first echo all echo build xv echo subdir tiff endif define passcdebugflag define ihavesubdir makesubdir subdir endif all echo build xv echo src dependtarget alltarget program normalprogramtarget xv deplib normalprogramtarget bggen deplib src alltarget program dependtarget ifdef ihavesubdir dependsubdir subdir endif normalprogramtarget xv deplib normalprogramtarget bggen deplib include file include if define sun define sysv include else include endif include include signal macro if define sun define sysv define sighold sigalrm define sigrelse sigalrm define sigpause sigalrm else define sigblock sigmask sigalrm define sigblock define sigpause endif default for most folk define random rand define srandom srand endif if define sun define sysv define random rand define srandom x srand x endif imakefile if hasgcc rm cc traditional o cflag rm else rm cc o cflag rm endif depend clean rm dependtarget 