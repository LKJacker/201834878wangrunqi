from barnett bruce barnett subject re title for xterm in article lanzo mark lanzo write hint for sun o user use instead of or csh built-in echo otherwise you have to embed literal esc and bel character in the string instead of use convenient octal sequence bad idea use be slower than the built-in echo thi be what i do it do execute hostname once per shell window and do read in one extra file but manipluating the title do not require execute extra program oh ye it do execute some program once per each system but it store them in a file for the next time if hostname then setenv hostname hostname endif figure how how to generate escape bell and echo command without a a line terminator i may have do thi before if so the variable e be set have i execute thi script before on thi system if e then echo already set the echo variable else if f echo hostname then source echo hostname else if echo n wc l then echo build in echo be bsd then berkeley style echo echo ech echo n echo hostname echo set e echo a tr a echo hostname echo set b echo a tr a echo hostname echo n echo hostname source echo hostname else echo build in echo be sysv echo ech echo echo hostname echo e echo hostname echo b echo hostname echo n c echo hostname source echo hostname endif are we use shelltool cmdtool or xterm duplicate these alias here to avoid problem if term sun then sun alias alia header ech e l e n alia iheader ech e l e n else if term xterm then alia header ech e b n alia iheader ech e b n endif bruce barnett barnett uunet barnett 