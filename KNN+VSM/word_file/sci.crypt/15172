from marc marc thibault subject re hard drive security for fbi target do a straight encryption of ymy keyring and put the result with mislead name somewhere they wo be notice eg in the window directory nobody know what half those file be do a straight encryption of a bat file that will decrypt the keyring to ramdisk and will set pgppath to point at it set up another bat file to decrypt and execute the first again on ram disk have it take the name of the target file a an argument so that there be no link between thi file and the non-existent batch file refer to by the rest of the system comment it so it look like a test script for fool around with pgp set pgppath to the pgp directory leave the original keyring that come with pgp in the directory with pgp a good indication that you be play with but have make seriou use of pgp add a set of key with ymy name and a really simple passphrase never use it or use it a ymy widely publish key for low-security e-mail when need run the second bat file make sure all intermediate and plainfile be generate on ramdisk when you hear the concussion grenade hit the power switch cheer marc marc thibault put another log marc nc freenet on the fire begin pgp public key block version hlnv end pgp public key block 