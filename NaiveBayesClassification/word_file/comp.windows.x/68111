from casper casper dik subject re problem with xdm under db danny backx write ye that be a bug in the port of xterm you sometime get thi when you start from xdm i have report thi to casper but do know whether it make it into hi set of patch yet basically in there be a test at line which check errno after attempt to open add the errno einval case to it and you should be fine it actually a bug in the solari kernel we could add a workaround to xterm open should never return einval but it do i do have time to update the patch lately but non of the exist problem seem to warrant a new set of patch since xsession be a bourne shell script anyway you might a well add a line a it be do in xdm coulda also keep the xdm environment variable it should have inherit it from the environment casper 