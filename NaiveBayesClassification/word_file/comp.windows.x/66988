from kaiser siegfried kaiser subject table widget cause trouble with xtquerygeometry content problem with table widget in the follow part of a program an user interface for a simulation system do work in but refuse to in of cause the do not know about the xptablewidgetclas we use tablewidgetclas instead catch from the net in time of and xptablechildposition formerly xttblposition since compile with the program cause a zero width or height error on sparc-station the trouble-shooter be the re computation of the table widget though it child label and asciitext widget exist xtquerygeometry return a prefer width and height of zero thu the follow asignment can not perform anything else but set the width and height of the newly create widget to zero no wonder xtpopup or xtmanage child create zero width or height error dependent on whether width and height of the shell widget be set in the resource file or not question do anyone know why xtquerygeometry return so low prefer value when work on table widget or perhap what to do about i be happy if someone be able to help me german be request to answer in german siegfried kaiser email kaiser the part of interest graphischer neuaufbau de model-init-formular storing the old width and height of the viewport widget which be the parent of the trouble table widget before the viewport widget be destroy the destroy of widget before resize them be a relict from if xtunmapwidget xtdestroywidget creating the subtree within the shell of which the root be the viewport widget n xtsetarg arg n xtnfromvert n xtsetarg arg n xtnfromhoriz n xtsetarg arg n xtnallowvert true n xtsetarg arg n xtnforcebar true n xtcreatewidget viewportwidgetclas arg n n xtcreatewidget table xptablewidgetclas arg n do create and position table widget child to position them it use xptable childposition in and xttblposition in the crucial function call intend to return the maximum height possible if there be sufficient space to show the whole table widget then the viewport shall grow a large a possible but not beyond the border of screen if there be enough space the window be intend to shrienk to the smallest possible height xtquerygeometry null pref according to the algorithm idea the new value of viewport widget height be select unfortunately lead to if else setting the new value to viewport widget ancestor w xtnametowidget xtresizewidget w width xtresizewidget width if in case the shell which contain view and allready exist be ha to be resize too n xtsetarg arg n xtnwidth n xtsetarg arg n xtnheight n xtsetarg arg n xtnborderwidth n xtgetvalue arg n xtresizewidget height end of if n xtsetarg arg n xtnwidth width n xtsetarg arg n xtnborderwidth bw n xtgetvalue arg n xtresizewidget width bw xtresizewidget if there be the shell height set within the resource file the program terminate within the first xtmanagechild on it second pa through the observe function the first pa succeed xtmanagechild xtmanagechild if in case the shell be pop up it ha to be do if there be no value set to the shell height within the resource file the program terminate here xtpopup xtgrabnone true end of if if someone suspect the creation of table widget content cause all the trouble there be the source of ptr table ptr widget table ptr be a linear list contain attribute and para meter of the model and additionally the correspon ding widget arg arg int n row col initialization of the local variable ptr col row schleife ueber die objekte bzw da pseudo-objekt fuer die parameter loop through the list of object and pseudo-object every object occur in the model ha zero or more attribute and some parameter which can be share by several object because of locality the attribute of one object be list in a second linear list of type wherea the parameter which can belong to any ob ject be put together in a pseudo-object thu the program loop through the list of object and pseudo-object and for each object through the list of it attribute resp parameter while null each object and pseudo-object be represent in a label widget n xtsetarg arg n xtnlabel name n xtcreatemanagedwidget object labelwidgetclas table arg n xptablechildposition col row col row ident schleife ueber die objekt-attribute bzw parameter while null each attribute and parameter be represent in a label and ha a correspond asciitext widget in which it be to be initialize n xtsetarg arg n xtnlabel name n xtcreatemanagedwidget ident labelwidgetclas table arg n xptablechildposition col row col n xtsetarg arg n xtnlength n xtsetarg arg n xtnstring text n xtsetarg arg n xtnedittype xawtextedit n xtsetarg arg n xtnwrap xawtextwrapword n xtsetarg arg n xtnresize xawtextresizeheight n xtsetarg arg n xtnusestringinplace true n xtcreatemanagedwidget text asciitextwidgetclas table arg n xptablechildposition col row col row end of while col end of while end of so far the problem in detail 