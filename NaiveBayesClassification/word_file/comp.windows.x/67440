from kelsey joe kelsey subject re xv ha escape in lindholm george lindholm write in article lindholm george lindholm write in article bradley john bradley write and here be the first patch the follow fix some problem with the imakefile file and make it possible to compile under solari without have to use the ucb stuff oop i do get the signal process right that what i get from do the work on an ascius terminal here be the fix patch the patch for need to use ifdef rather than the overly complex if define sun define sysv in order to generically compile xv on also rand and srand do work very well use and to get better result otherwise compile just fine on my esix system v box with and i just uncommented the gcc definition in the makefile add to the copt line and modify lib to include lsocket lnsl really john if you just use the imakefile thing like thi do even have to get mention signal macro ifdef define sighold sigalrm define sigrelse sigalrm define sigpause sigalrm else define sigblock sigmask sigalrm define sigblock define sigpause endif default for most folk define random rand define srandom srand endif ifdef define random define srandom endif 