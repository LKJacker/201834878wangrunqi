from olcay boz subject re postscript view for do or window where can i find the m window version of ghostscript thank in article hjstein l harvey j stein write i be use version of ghostscript and i quite satisfy with it there be actually version a plain do version a version and a window version harvey stein hjstein m 