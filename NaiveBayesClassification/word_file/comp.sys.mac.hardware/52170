from hade brian v hughe subject re quadra armani anand mani write beside the faster processor the ha scsi and a far a i know faster due to separate processor handle those function mostly the do have a controller chip a it scsi chip but it do not have the chip to use the wide or fast mode and therefore do offer much more in the way of scsi performance over the it do howrever have a faster controller than the thi be where the real speed boost come from hade 